diff --git a/packages/app/src/live-voice/live-voice-footer-button.test.tsx b/packages/app/src/live-voice/live-voice-footer-button.test.tsx
index 24d12e324..04ef7ac4e 100644
--- a/packages/app/src/live-voice/live-voice-footer-button.test.tsx
+++ b/packages/app/src/live-voice/live-voice-footer-button.test.tsx
@@ -109,6 +109,7 @@ vi.mock("@/components/ui/tooltip", () => ({
 vi.mock("react-native-unistyles", () => ({
   StyleSheet: { create: () => ({}) },
   withUnistyles: <T,>(component: T) => component,
+  useUnistyles: () => ({ rt: { breakpoint: "lg" } }),
 }));

 vi.mock("lucide-react-native", () => {
@@ -163,7 +163,8 @@ describe("LiveVoiceFooterButton", () => {
   });
 
   function render(): void {
-    act(() => root.render(<LiveVoiceFooterButton />));
+    // `active` arrives from the sidebar footer once that topic is stacked.
+    act(() => root.render(<LiveVoiceFooterButton active />));
   }
 
   function query(testID: string): HTMLElement {
diff --git a/packages/app/src/live-voice/live-voice-runtime.ts b/packages/app/src/live-voice/live-voice-runtime.ts
index 8399037f9..98336c8f7 100644
--- a/packages/app/src/live-voice/live-voice-runtime.ts
+++ b/packages/app/src/live-voice/live-voice-runtime.ts
@@ -455,7 +455,11 @@ export function createLiveVoiceRuntime(deps: LiveVoiceRuntimeDeps): LiveVoiceRun
     patch({ isMuted: muted });
   }
 
-  async function startCall(call: LiveVoiceCall, serverId: string, options?: { contextProfileId?: string }): Promise<void> {
+  async function startCall(
+    call: LiveVoiceCall,
+    serverId: string,
+    options?: { contextProfileId?: string },
+  ): Promise<void> {
     try {
       if (!isCurrent(call)) return;
       call.unsubscribe = call.client.subscribeUpdates((message) => handleUpdate(call, message));
@@ -548,7 +552,7 @@ export function createLiveVoiceRuntime(deps: LiveVoiceRuntimeDeps): LiveVoiceRun
     getSnapshot() {
       return snapshot;
     },
-    async start(serverId) {
+    async start(serverId, options) {
       if (snapshot.phase === "starting" || snapshot.phase === "active") {
         throw new LiveVoiceStartError({ code: "already_active", message: null });
       }
@@ -589,7 +593,7 @@ export function createLiveVoiceRuntime(deps: LiveVoiceRuntimeDeps): LiveVoiceRun
       };
       currentCall = call;
       publish({ ...IDLE_SNAPSHOT, phase: "starting", serverId });
-      await startCall(call, serverId);
+      await startCall(call, serverId, options);
     },
     async stop() {
       const call = currentCall;
