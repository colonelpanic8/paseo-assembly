diff --git a/packages/app/src/hooks/use-settings/app-settings.ts b/packages/app/src/hooks/use-settings/app-settings.ts
index 178d3b18a..ab96e13cd 100644
--- a/packages/app/src/hooks/use-settings/app-settings.ts
+++ b/packages/app/src/hooks/use-settings/app-settings.ts
@@ -1,7 +1,7 @@
 import { useCallback, useMemo } from "react";
-import AsyncStorage from "@react-native-async-storage/async-storage";
 import { useQuery, useQueryClient, type QueryClient } from "@tanstack/react-query";
 import { queryClient as appQueryClient } from "@/data/query-client";
+import { layeredSettingsStorage } from "@/storage/settings-seed";
 import {
   APP_SETTINGS_KEY,
   APP_SETTINGS_QUERY_KEY,
@@ -18,7 +18,7 @@ import {
  * bridge, and that module graph does not survive being imported from a plain unit test. Anything
  * that only needs an `AppSettings` key imports this instead.
  */
-const productionDeps: AppSettingsDeps = { storage: AsyncStorage };
+const productionDeps: AppSettingsDeps = { storage: layeredSettingsStorage };
 
 export interface UseAppSettingsReturn {
   settings: AppSettings;
@@ -53,7 +53,7 @@ export function useAppSettings(): UseAppSettingsReturn {
     try {
       const next = { ...DEFAULT_CLIENT_SETTINGS };
       queryClient.setQueryData<AppSettings>(APP_SETTINGS_QUERY_KEY, next);
-      await AsyncStorage.setItem(APP_SETTINGS_KEY, JSON.stringify(next));
+      await layeredSettingsStorage.setItem(APP_SETTINGS_KEY, JSON.stringify(next));
     } catch (err) {
       console.error("[AppSettings] Failed to reset settings:", err);
       throw err;
