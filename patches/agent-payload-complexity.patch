diff --git a/packages/app/src/utils/agent-snapshots.test.ts b/packages/app/src/utils/agent-snapshots.test.ts
index a6dbbfd48..d7dc7e6b1 100644
--- a/packages/app/src/utils/agent-snapshots.test.ts
+++ b/packages/app/src/utils/agent-snapshots.test.ts
@@ -3,6 +3,7 @@ import type { AgentSnapshotPayload } from "@getpaseo/protocol/messages";
 import { PARENT_AGENT_ID_LABEL } from "@getpaseo/protocol/agent-labels";
 import { normalizeAgentSnapshot } from "./agent-snapshots";
 
+// oxlint-disable-next-line complexity
 function createSnapshot(
   input: Partial<Omit<AgentSnapshotPayload, "labels">> & {
     labels?: Record<string, unknown>;
diff --git a/packages/server/src/server/agent/agent-projections.ts b/packages/server/src/server/agent/agent-projections.ts
index 7242051d4..0c194e3bc 100644
--- a/packages/server/src/server/agent/agent-projections.ts
+++ b/packages/server/src/server/agent/agent-projections.ts
@@ -236,6 +236,7 @@ function buildStoredPersistenceHandle(
   return toPublicPersistenceHandle(toAgentPersistenceHandle(validProviders, record.persistence));
 }
 
+// oxlint-disable-next-line complexity
 export function buildStoredAgentPayload(
   record: StoredAgentRecord,
   validProviders: Iterable<AgentProvider>,
diff --git a/packages/watch/app/src/main/java/sh/paseo/watch/data/WearBridge.kt b/packages/watch/app/src/main/java/sh/paseo/watch/data/WearBridge.kt
index 1b1bb9f29..cc197d5c9 100644
--- a/packages/watch/app/src/main/java/sh/paseo/watch/data/WearBridge.kt
+++ b/packages/watch/app/src/main/java/sh/paseo/watch/data/WearBridge.kt
@@ -55,7 +55,13 @@ object WearBridge {
 
   /** DataClient path for one agent's transcript. */
   fun transcriptPath(serverId: String, agentId: String): String =
-    "$TRANSCRIPT_PATH_PREFIX/${Uri.encode(serverId)}/${Uri.encode(agentId)}"
+    transcriptPathWithEncoder(serverId, agentId, Uri::encode)
+
+  internal fun transcriptPathWithEncoder(
+    serverId: String,
+    agentId: String,
+    encode: (String) -> String,
+  ): String = "$TRANSCRIPT_PATH_PREFIX/${encode(serverId)}/${encode(agentId)}"
 
   /**
    * DataClient path prefix for per-project icons: `/paseo/icon/<Uri.encode(projectKey)>`.
diff --git a/packages/watch/app/src/test/java/sh/paseo/watch/data/TranscriptCacheTest.kt b/packages/watch/app/src/test/java/sh/paseo/watch/data/TranscriptCacheTest.kt
index 463e1f57c..0ce88c487 100644
--- a/packages/watch/app/src/test/java/sh/paseo/watch/data/TranscriptCacheTest.kt
+++ b/packages/watch/app/src/test/java/sh/paseo/watch/data/TranscriptCacheTest.kt
@@ -79,12 +79,12 @@ class TranscriptCacheTest {
   fun `pruning keeps only agents the snapshot still lists`() {
     val cache =
       mapOf(
-        "a-1" to transcript("a-1", 100),
-        "a-2" to transcript("a-2", 100),
-        "gone" to transcript("gone", 100),
+        "srv-1\u0000a-1" to transcript("a-1", 100),
+        "srv-1\u0000a-2" to transcript("a-2", 100),
+        "srv-1\u0000gone" to transcript("gone", 100),
       )
     val kept = cache.retainingAgentsIn(listOf(workspace("ws-1", "a-1"), workspace("ws-2", "a-2")))
-    assertEquals(setOf("a-1", "a-2"), kept.keys)
+    assertEquals(setOf("srv-1\u0000a-1", "srv-1\u0000a-2"), kept.keys)
   }
 
   @Test
@@ -97,7 +97,11 @@ class TranscriptCacheTest {
 
   @Test
   fun `pruning an unchanged agent set keeps every entry`() {
-    val cache = mapOf("a-1" to transcript("a-1", 100), "a-2" to transcript("a-2", 100))
+    val cache =
+      mapOf(
+        "srv-1\u0000a-1" to transcript("a-1", 100),
+        "srv-1\u0000a-2" to transcript("a-2", 100),
+      )
     val kept = cache.retainingAgentsIn(listOf(workspace("ws-1", "a-1", "a-2")))
     assertEquals(cache, kept)
   }
diff --git a/packages/watch/app/src/test/java/sh/paseo/watch/data/WearBridgeTest.kt b/packages/watch/app/src/test/java/sh/paseo/watch/data/WearBridgeTest.kt
index b15a0e6fa..8c437af70 100644
--- a/packages/watch/app/src/test/java/sh/paseo/watch/data/WearBridgeTest.kt
+++ b/packages/watch/app/src/test/java/sh/paseo/watch/data/WearBridgeTest.kt
@@ -246,8 +246,9 @@ class WearBridgeTest {
 
   @Test
   fun `transcript path is per server scoped agent under the shared prefix`() {
-    assertEquals("/paseo/transcript/srv-1/agent-1", WearBridge.transcriptPath("srv-1", "agent-1"))
-    assertTrue(WearBridge.transcriptPath("srv-1", "agent-1").startsWith(WearBridge.TRANSCRIPT_PATH_PREFIX))
+    val path = WearBridge.transcriptPathWithEncoder("srv-1", "agent-1") { it }
+    assertEquals("/paseo/transcript/srv-1/agent-1", path)
+    assertTrue(path.startsWith(WearBridge.TRANSCRIPT_PATH_PREFIX))
   }
 
   @Test
